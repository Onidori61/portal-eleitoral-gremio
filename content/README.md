# Conteúdo editável

Edite os textos públicos em `textos.js`, `mensagens.js` e `documentos.js`.
Não altere regras de votação diretamente no frontend: elas pertencem a `config/eleicao.js`
e devem refletir o edital/regimento vigente.
