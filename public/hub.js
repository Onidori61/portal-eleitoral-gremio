const $ = (id) => document.getElementById(id);
const request = async (path, body) => { const response = await fetch(path, { method: "POST", headers: { "content-type": "application/json", authorization: `Bearer ${$("token").value}` }, body: JSON.stringify(body) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Falha na operação."); return data; };
$("import").addEventListener("click", async () => { try { const file = $("csv").files[0]; if (!file) throw new Error("Selecione um CSV."); const result = await request("/api/admin/import-voters", { csv: await file.text() }); $("import-message").textContent = `${result.imported} eleitores importados.`; } catch (error) { $("import-message").textContent = error.message; } });
$("upload").addEventListener("click", async () => { try { const file = $("image").files[0]; if (!file) throw new Error("Selecione uma imagem."); const image = (await new Promise((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(reader.result.split(",")[1]); reader.onerror = reject; reader.readAsDataURL(file); })); const result = await request("/api/upload", { image, name: file.name }); $("upload-message").textContent = `Imagem enviada: ${result.url}`; } catch (error) { $("upload-message").textContent = error.message; } });
