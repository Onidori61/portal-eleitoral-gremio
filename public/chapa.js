const escapeHtml = (value) => String(value || "").replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#039;" }[char]));
const root = document.getElementById("slate-detail");
const render = (slate) => {
  const members = slate.integrantes || [];
  root.innerHTML = `<section class="slate-profile"><div class="slate-profile-top"><div><span class="profile-kicker">PERFIL DA CANDIDATURA</span><div class="profile-title-line"><span class="profile-number">CHAPA ${escapeHtml(slate.numero)}</span><h1>${escapeHtml(slate.nome)}</h1></div><span class="profile-status"><i></i> Candidatura homologada</span></div>${slate.imagemUrl ? `<img class="slate-profile-mark" src="${escapeHtml(slate.imagemUrl)}" alt="Identidade visual da ${escapeHtml(slate.nome)}">` : ""}</div><p class="slate-detail-presentation">${escapeHtml(slate.apresentacao || "Esta chapa ainda não publicou uma apresentação.")}</p><div class="profile-grid"><div class="profile-main"><div class="detail-block"><div class="detail-heading"><span class="material-symbols-outlined">groups</span><div><span class="profile-kicker">COMPOSIÇÃO</span><h2>Integrantes da chapa</h2></div></div><div class="detail-members">${members.map((member) => `<article><span class="member-avatar material-symbols-outlined">person</span><div><strong>${escapeHtml(member.nome)}</strong><span>${escapeHtml(member.cargo)}${member.turma ? ` · ${escapeHtml(member.turma)}` : ""}</span>${member.apresentacao ? `<p>${escapeHtml(member.apresentacao)}</p>` : ""}</div></article>`).join("") || "<p>Nenhum integrante publicado.</p>"}</div></div><div class="detail-block"><div class="detail-heading"><span class="material-symbols-outlined">fact_check</span><div><span class="profile-kicker">PLANO DE GESTÃO</span><h2>Propostas</h2></div></div><div class="detail-proposals">${(slate.propostas || []).map((proposal, index) => `<article><span class="proposal-index">${String(index + 1).padStart(2, "0")}</span><div><h3>${escapeHtml(proposal.titulo)}</h3><p>${escapeHtml(proposal.descricao)}</p></div></article>`).join("") || "<p>Esta chapa ainda não publicou propostas.</p>"}</div></div></div><aside class="profile-aside"><div class="profile-aside-card"><span class="material-symbols-outlined">how_to_vote</span><strong>Confira antes de votar</strong><p>Leia a apresentação, conheça os integrantes e consulte as propostas desta candidatura.</p><a class="official-button dark" href="/#votar">Ir para votação →</a></div>${(slate.redes || []).length ? `<div class="detail-block profile-links"><div class="detail-heading"><span class="material-symbols-outlined">link</span><h2>Redes sociais</h2></div><div class="detail-links">${slate.redes.map((social) => `<a href="${escapeHtml(social.url)}" target="_blank" rel="noreferrer">${escapeHtml(social.plataforma)} ↗</a>`).join("")}</div></div>` : ""}</aside></div></section>`;
};
try {
  const id = new URLSearchParams(location.search).get("id");
  const response = await fetch("/api/public");
  const data = await response.json();
  const slate = (data.slates || []).find((item) => item.id === id);
  if (!slate) throw new Error("Chapa não encontrada ou ainda não publicada.");
  document.title = `${slate.nome} | Portal Eleitoral`;
  document.getElementById("school-name").textContent = data.school?.nome || "Grêmio Estudantil";
  render(slate);
} catch (error) { root.innerHTML = `<p class="empty-state">${escapeHtml(error.message)}</p>`; }
